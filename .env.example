# Database URL
# Local development: file path to SQLite database
DATABASE_URL="file:./db/custom.db"

# Turso SQLite for Production (Vercel deployment)
# Uncomment this line when deploying to Vercel and set your Turso URL
# DATABASE_URL="libsql://your-db-url.turso.io?authToken=your-jwt-token"

# NextAuth Secret (generate with: openssl rand -base64 32)
NEXTAUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"

# NextAuth URL (set this in production)
NEXTAUTH_URL="http://localhost:3000"
# For Vercel: NEXTAUTH_URL="https://your-domain.vercel.app"
